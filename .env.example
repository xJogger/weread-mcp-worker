# Local reference only. Do not put real values in files committed to Git.
# For deployed Workers, use `wrangler secret put` instead.
WEREAD_MCP_ENCRYPTION_KEY="generate-a-random-value-with-scripts/generate-secret.mjs"
WEREAD_MCP_SETUP_PASSWORD="change-me"
WEREAD_SKILL_VERSION="1.0.3"
